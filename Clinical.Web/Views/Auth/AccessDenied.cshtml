@{
    ViewData["Title"] = "Acceso denegado";
}
<div class="d-flex flex-column align-items-center justify-content-center min-vh-60">
    <div class="text-center">
        <div class="denied-icon"><i class="bi bi-shield-x"></i></div>
        <h2 class="fw-bold mt-3">Acceso denegado</h2>
        <p class="text-muted">No tienes permisos para acceder a esta sección.</p>
        <a asp-controller="Dashboard" asp-action="Index" class="btn btn-primary mt-2">
            <i class="bi bi-house me-2"></i>Volver al dashboard
        </a>
    </div>
</div>
